# 2026-06-02

## L3 商业模式乘数 Bug 修复
- **问题**: `_estimate_l3()` 用 `head(1)` 取第一条 fina_indicator 记录，该记录可能是季报而非年报，导致 ROE 偏低（茅台 Q1 ROE=10.57% vs 年报 34.46%），L3 乘数误判为 ×0.8
- **根因**: Tushare `fina_indicator` 按 end_date 倒序排列，年报和季报混在同一个 DataFrame
- **修复**:
  1. 优先筛选 end_date 以 "1231" 结尾的年报数据
  2. 若无年报，基于季报推测全年 ROE：Q1×4, H1×2, Q3×4/3
  3. 文件: `src/calculator/turtle_strategy/scoring.py` `_estimate_l3()`
- **效果**: 茅台 L3 ×0.8 → ×1.2, Final 11.06 → 16.60

## DC 公式 v0.19 → v0.20：stot_out_inv_act → c_pay_acq_const_fiolta
- **问题**: `stot_out_inv_act`（投资活动现金流出小计）包含理财/存单等可逆资金配置（茅台该字段 143.57 亿，其中 137.5 亿是买理财），导致 DC 被严重低估
- **修复**: 改为 `c_pay_acq_const_fiolta`（购建固定资产无形资产支付的现金），只扣真正的维持性 CAPEX
- **保留项**: `fin_expense`（op_cf 是息前概念，fin_expense 是债务优先索取权定价）; `trad_assets`（正当地反映可随时变现的金融资产）
- **改动文件**: `disposable_cash.py`（6 处）; `turtle_constants.yaml`（公式描述 + 版本号 v0.20）

## v0.21：分配比率外推加回分红修正循环悖论
- **问题**: `_extrapolate_from_history()` 中 `ratio = total_div / DC`，但 DC 的 money_cap 是年末余额已被分红抽干，形成循环（分红越多→DC越小→ratio虚高，早年可达 133%）
- **修复**: `adjusted_dc = dc + total_div`（把分红加回年末现金），再用 `total_div / adjusted_dc` 算比率，ratio 天然 ≤ 100%
- **改动文件**: `pr_calculator.py`（_extrapolate_from_history 一行 + 版本号 v0.19→v0.21）; `src/__init__.py`（v0.18→v0.21）; `constants/__init__.py`（v0.18→v0.21）
- **MEMORY.md**: 更新版本号和方法论记录

## 项目管理文件更新至 v0.21 + GitHub 推送 (18:12)
- **PROJECT_STATUS.md**: v0.19→v0.21，描述更新，代码量 7000→7500
- **CHANGELOG.md**: 新增 v0.20/v0.21 条目
- **README.md**: 版本号 v0.19→v0.21
- **pyproject.toml**: version 0.1.0→0.21.0
- **TRACEABILITY.md**: 日期 2026-06-01→2026-06-02
- **Git**: commit de68515 → push main 到 GitHub

## 全量代码提交 (18:14)
- 59 files changed: v0.20 DC修正 + v0.21 分配比率外推修正 + 补齐之前缺失的12模块骨架（agents/checkpoint+context+coordinator, calculator/financial_ratios+registry+cash_recon+constants_turtle+sotp_adjust, data_fetcher/base+web, llm/cache+manager+prompt_builder+provider+schema+local_analysis_engine, reporter/renderer+templates, rules/injector+validator, screener/stock_pool, tests/conftest+fixtures, scripts/, backtest_report.html, report_600519_SH.html）
- **GitHub**: `21882a8` → `de68515..21882a8 main -> main` ✅

## v0.22：DC 扣除成长性投入 + PR 真实数学回报 (23:19)
- **DC 公式扩展** (`disposable_cash.py`):
  - 新增 `- c_pay_acq_subsidiary`（并购子公司支付的现金）
  - 新增 `- max(0, 年末长投 - 年初长投)`（参股净增额，仅新增时才扣）
  - 所有成长性投入（建厂+并购+参股）全部扣除，理财等可逆资金不被误扣
- **分配比率改版** (`pr_calculator.py`):
  - 从 `median(分红/DC) × 0.7` 改为 `mean(分红/净利润)` 算术平均
  - 分母独立于 DC，消除 v0.21 的循环依赖
  - 一档公告承诺不再 × 0.8
- **PR 公式简化**: 去除安全边际系数和红利税折扣
  - `dividend_withholding: 0.10 → 0.0`
  - PR = (DC × 分配比率 + 回购) / 市值（纯数学真实回报）
- **报告**: 添加黄框备注「持股<1月需缴10%红利税」
- **改动文件**: disposable_cash.py, pr_calculator.py, turtle_constants.yaml, schemas.py, models.py, report_generator.py, analysis_report.html, CHANGELOG.md, PROJECT_STATUS.md

## v0.22 Bug 修复：分红重复计数 + total_share 降级查找 (23:31)
- **分红重复计数**: Tushare 同一分红有「股东大会通过」和「实施」两条记录，`_extrapolate_from_history`中对两条都累加，导致每股分红翻倍（茅台 2024 从 51.56 变成 130.73 元/股），分配比率虚高到 133.7%
- **修复**: `div_proc != "实施"` → continue（只取实际实施的分红）
- **income 去重**: `drop_duplicates(subset=["end_date"])`（Tushare 2022 年报返回了重复行）
- **total_share 降级**: 茅台 2022/2023 的 daily_basic 无 1231 日期 → 新增 fallback2：该年任意交易日
- **茅台 v0.22 结果**: PR=4.33%, 分配比率=65.9%, DC=1075亿
