# Stock Analysis Framework — CodeBuddy 项目规则

---

## 1. 技术约定
- Python 3.11+, Poetry 包管理
- Pydantic v2 数据模型，双轨Schema（核心字段 + RawTushareData）
- loguru 结构化日志
- tenacity 重试装饰器
- 所有公共API必须有类型注解（mypy strict）
- ruff line-length=100, target-version=py39

## 2. 文件命名
- Python 模块: snake_case
- 测试文件: test_<module>.py
- YAML 规则: snake_case.yaml

## 3. 测试约定
- pytest, 覆盖率目标 ≥80%
- 单元测试放 tests/unit/, 集成测试放 tests/integration/
- Mock Tushare 调用，不依赖真实API

## 4. 项目管理文件层级

| 优先级 | 文件 | 职责 |
|:--:|------|------|
| 1 | `docs/plan.md` | 设计决策的唯一权威来源。架构、公式、模块设计。 |
| 2 | `TRACEABILITY.md` | 设计→实现→测试 三层映射。每项的实现/连通/测试状态。 |
| 3 | `pyproject.toml` | 版本号 + 依赖。 |
| 4 | `PROJECT_STATUS.md` | 五阶段进度 + 版本历史。 |
| 5 | `CHANGELOG.md` | 每个版本的 Added/Changed/Removed。 |
| 6 | `README.md` | 对外门面。架构概览、快速开始。 |
| 7 | `ISSUES.md` | 已知问题 + 待办。 |

## 5. 开发工作流（严格执行）

### 总原则：文档先行，代码后行

任何改动都必须在写代码之前先更新对应的项目管理文件。
改动分为三级：

- **Hotfix** (修bug)：跳过阶段一全部 + 阶段二除 CHANGELOG
- **Minor** (小优化)：跳过 plan.md + README.md
- **Major** (新功能/重构)：全流程

### 阶段〇：准备
1. 阅读当前 `TRACEABILITY.md` + `docs/plan.md` + `PROJECT_STATUS.md`
2. 运行 `python scripts/verify_traceability.py` 确认基线状态
3. 确认本次改动级别（Hotfix / Minor / Major）

### 阶段一：设计文档（先于任何代码）
4. **`docs/plan.md`**：记录设计决策、公式变更、新增/删除模块、目录结构变更
5. **`TRACEABILITY.md`**：新增/修改/删除条目，预标状态（新增项标 ⚠️，删除项移除）
6. **`pyproject.toml`**：如果涉及版本升级，同步 `version` 和 `description`
7. **`scripts/verify_traceability.py`**：如果 TRACEABILITY.md 新增/删除条目，同步更新 `ITEMS` 列表

### 阶段二：元数据同步
8. **`PROJECT_STATUS.md`**：更新阶段描述、版本号、测试数据、代码量
9. **`CHANGELOG.md`**：新增版本条目（Added / Changed / Removed / Fixed）
10. **`README.md`**（仅 Major）：如果架构/API/阶段描述变更，更新
11. **`ISSUES.md`**（按需）：如果有新的已知问题或旧问题修复，更新

### 阶段三：记忆
12. **`MEMORY.md`**：更新持久事实（版本号、核心公式、技术栈变更）
13. **`YYYY-MM-DD.md`**：记录本次会话改动摘要

### 阶段四：实现
14. 写/改源代码
15. 写/改测试

### 阶段五：验证（硬门控，任一失败不得提交）
16. `python -m ruff check src/ tests/` — 零错误
17. `python -m mypy src/` — 零错误
18. `python -m pytest tests/ -v` — 全部通过
19. `python scripts/verify_traceability.py` — 连通性 + 测试覆盖检查通过

### 阶段六：收尾
20. 回溯 `TRACEABILITY.md`：将本次涉及的条目状态从 ⚠️ 改为 ✅
21. 更新 `TRACEABILITY.md` 底部的统计行和日期
22. `git add` 所有变更文件
23. `git commit -m "vX.Y: <简短描述>"`
24. `git push origin main`

## 6. 版本号清单

每次版本升级（如 v0.22 → v0.23）必须更新以下 7 处：
1. `pyproject.toml` → `version` + `description`
2. `PROJECT_STATUS.md` → `**当前版本**`
3. `CHANGELOG.md` → 新增 `## [vX.Y]` 条目
4. `README.md` → 标题中的版本号 + 阶段描述
5. `TRACEABILITY.md` → 最后更新日期 + 统计行版本号
6. `src/reporter/report_generator.py` → 模板中的版本号（页脚 + 管线标题）
7. `src/cli.py` → `print(f"[Turtle] v{version}")`

## 7. TRACEABILITY.md 三列约定

| 列 | ❌ | ⚠️ | ✅ | — |
|----|----|----|----|----|
| **实现** | 文件不存在 | 文件存在但空壳/占位 | 功能按 plan.md 完整实现 | |
| **连通** | 零生产引用 | 仅被测试引用 | 被生产代码 import（接入管线） | 实现 ❌ 时不适用 |
| **测试** | 无对应测试文件 | | 存在 `test_<module>.py` 且 test 函数 > 0 | 非代码项不适用 |

`verify_traceability.py` 自动检测连通和测试列，实现列需人工判定。

## 8. Git Commit 格式

```
<type>: <简短描述>

<详细说明（可选）>

- 变更点1
- 变更点2
```

**type**：`feat` / `fix` / `refactor` / `docs` / `test` / `chore`

示例：
```
feat: v0.23 L3 十二维商业模式评估 + 加法百分制

- 评分公式: Final = L3(30pt) + L4(45pt) + L5(25pt) = 100pt
- 新增 l3_calculator.py (12-dim, ~320行)
- L5 重构为纯估值安全边际
- L2 降级为纯门控
```

## 9. 分阶段开发
- 五阶段严格按序推进
- 每阶段产出可测试最小单元
- 确定性层（Python）与智能层（LLM）分离
